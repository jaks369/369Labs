import { evaluateNode, ConditionNode, EvalContext, legacyConditionToNode, lastDigitOf } from "./conditionEval";import { Tick, DerivContractType } from "./derivWebSocket";export interface BacktestTrade {  entryTime: number;  entryPrice: number;  exitTime: number;  exitPrice: number;  contractType: string;  result: "win" | "loss";  pnl: number;}export interface BacktestResult {  totalTrades: number;  wins: number;  losses: number;  winRate: number;  totalPnl: number;  maxDrawdown: number;  trades: BacktestTrade[];  equityCurve: number[];}function lastDigit(price: number): number {  return parseInt(price.toFixed(2)[price.toFixed(2).length - 1], 10);}function evaluateCondition(rule: StrategyRule, tick: Tick, history: Tick[]): boolean {  const indicator = rule.condition.indicator;  const count = rule.condition.count;  if (history.length < count) return false;  // Check if condition was met at a specific index in history  const checkIndex = (idx: number): boolean => {    const t = history[idx];    switch (indicator) {      case "digit_over": return lastDigit(t.price) > (rule.condition.barrier ?? 5);      case "digit_under": return lastDigit(t.price) < (rule.condition.barrier ?? 5);      case "digit_even": return lastDigit(t.price) % 2 === 0;      case "digit_odd": return lastDigit(t.price) % 2 === 1;      case "consecutive_rise": return idx > 0 && t.price > history[idx - 1].price;      case "consecutive_fall": return idx > 0 && t.price < history[idx - 1].price;      default: return false;    }  };  if (rule.condition.comparison === "appears_consecutively") {    for (let i = history.length - count; i < history.length; i++) {      if (!checkIndex(i)) return false;    }    return true;  }  // "appears" (frequency)  const windowStart = Math.max(0, history.length - 20);  let occurrences = 0;  for (let i = windowStart; i < history.length; i++) {    if (checkIndex(i)) occurrences++;  }  return occurrences >= count;}function simulateTradeOutcome(entryPrice: number, nextPrice: number, contractType: string, barrier?: number): "win" | "loss" {  switch (contractType) {    case "CALL": return nextPrice > entryPrice ? "win" : "loss";    case "PUT": return nextPrice < entryPrice ? "win" : "loss";    case "DIGITEVEN": return lastDigit(nextPrice) % 2 === 0 ? "win" : "loss";    case "DIGITODD": return lastDigit(nextPrice) % 2 === 1 ? "win" : "loss";    case "DIGITOVER": return lastDigit(nextPrice) > (barrier ?? 5) ? "win" : "loss";    case "DIGITUNDER": return lastDigit(nextPrice) < (barrier ?? 5) ? "win" : "loss";    default: return nextPrice > entryPrice ? "win" : "loss";  }}function calcPnl(result: "win" | "loss", stake: number): number {  return result === "win" ? stake * 0.95 : -stake;}export async function runBacktest(  ticks: Tick[],  strategy: StrategyRule,  stake: number): Promise<BacktestResult> {  const trades: BacktestTrade[] = [];  const history: Tick[] = [];  let balance = 0;  // Map action to contract type  let contractType: string;  let barrier: number | undefined;  switch (strategy.action.tradeType) {    case "buy_rise": contractType = "CALL"; break;    case "buy_fall": contractType = "PUT"; break;    case "buy_even": contractType = "DIGITEVEN"; break;    case "buy_odd": contractType = "DIGITODD"; break;    case "buy_over": contractType = "DIGITOVER"; barrier = strategy.condition.barrier ?? 5; break;    case "buy_under": contractType = "DIGITUNDER"; barrier = strategy.condition.barrier ?? 5; break;    default: contractType = "CALL";  }  for (let i = 0; i < ticks.length; i++) {    history.push(ticks[i]);    if (evaluateCondition(strategy, ticks[i], history)) {      const entryTime = ticks[i].timestamp;      const entryPrice = ticks[i].price;      const exitIdx = i + 1;      if (exitIdx >= ticks.length) break;      const exitPrice = ticks[exitIdx].price;      const result = simulateTradeOutcome(entryPrice, exitPrice, contractType, barrier);      const pnl = calcPnl(result, stake);      balance += pnl;      trades.push({ entryTime, entryPrice, exitTime: ticks[exitIdx].timestamp, exitPrice, contractType, result, pnl });      i = exitIdx;    }  }  const wins = trades.filter(t => t.result === "win").length;  const losses = trades.filter(t => t.result === "loss").length;  let peak = 0;  let maxDrawdown = 0;  let runningPnl = 0;  const equityCurve: number[] = [];  for (const t of trades) {    runningPnl += t.pnl;    equityCurve.push(runningPnl);    if (runningPnl > peak) peak = runningPnl;    const dd = peak - runningPnl;    if (dd > maxDrawdown) maxDrawdown = dd;  }  return {    totalTrades: trades.length,    wins,    losses,    winRate: trades.length > 0 ? (wins / trades.length) * 100 : 0,    totalPnl: balance,    maxDrawdown,    trades,    equityCurve,  };}